{
  "name": "thermostat",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server", "simulator"],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:simulator": "npm run dev --workspace=simulator"
  }
}